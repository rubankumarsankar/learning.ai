{
  "id": 116,
  "title": "Hyperparameters: GridSearchCV vs RandomizedSearchCV",
  "content": "### Day 116: Hyperparameters: GridSearchCV vs RandomizedSearchCV\n\n**Hyperparameter Tuning:** Finding optimal model settings.\n- **GridSearchCV:** Try ALL combinations (exhaustive but slow)\n- **RandomizedSearchCV:** Try N random combos (faster, often equally good)\n- Both use cross-validation internally",
  "codeExample": "# Day 116: Hyperparameters: GridSearchCV vs RandomizedSearchCV\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.model_selection import GridSearchCV, RandomizedSearchCV, train_test_split\nimport numpy as np\n\nnp.random.seed(42)\nX = np.random.randn(200, 5)\ny = (X[:, 0] + X[:, 1] > 0).astype(int)\nX_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)\n\n# GridSearchCV\nparam_grid = {'n_estimators': [50, 100], 'max_depth': [3, 5, 10]}\ngrid = GridSearchCV(RandomForestClassifier(random_state=42),\n                    param_grid, cv=3, scoring='accuracy')\ngrid.fit(X_train, y_train)\n\nprint(f\"Best params: {grid.best_params_}\")\nprint(f\"Best CV score: {grid.best_score_:.3f}\")\nprint(f\"Test score: {grid.score(X_test, y_test):.3f}\")",
  "practiceQuestions": [
    "Grid vs Randomized search?",
    "What is a hyperparameter vs parameter?",
    "How does GridSearchCV use CV?",
    "When to use RandomizedSearchCV?"
  ],
  "keywords": [
    "Hyperparameter Tuning",
    "GridSearchCV",
    "RandomizedSearchCV"
  ],
  "keyTakeaways": [
    "GridSearchCV: Try ALL combinations (exhaustive but slow)",
    "RandomizedSearchCV: Try N random combos (faster, often equally good)",
    "Both use cross-validation internally"
  ]
}